// Re-exports the shared catalog so all existing client imports remain unchanged.
// The source of truth lives in shared/catalog.ts — edit there, not here.
export * from "../../../../shared/catalog";
